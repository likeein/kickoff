<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.teamcommit.kickoff.Service.GameMapper">
    <select id="gameList" parameterType="com.teamcommit.kickoff.Do.GameDO">
        <![CDATA[
        SELECT *
        FROM
            GAME

        ]]>
    </select>

    <insert id = "selectGame" parameterType="com.teamcommit.kickoff.Do.GameDO">
        <![CDATA[
        INSERT INTO GAME
        (
            GAME_SEQNO,
            PLACE_ID,
            TEAM1_ID,TEAM2_ID,
            GAME_START_TIME,
            GAME_STYLE,
            GAME_GENDER,
            GAME_STATUS
        )
        VALUES(
                  GAME_SEQ.NEXTVAL,
                  #{placeId},
                  #{team1Id},
                  #{tema2Id},
                  #{gameStartTime},
                  #{gameStyle},
                  #{gameGender},
                  #{gameStatus}
              )
        ]]>
    </insert>
</mapper>