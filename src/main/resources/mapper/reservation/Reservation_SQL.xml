<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.teamcommit.kickoff.Service.ReservationMapper">

    <!-- Member Info -->

    <!-- Reservation List -->
    <select id="selectReservationList" parameterType="com.teamcommit.kickoff.Do.ReservationDO" resultType="com.teamcommit.kickoff.Do.ReservationDO">
        <![CDATA[
            SELECT *
            FROM
                RESERVATION
        ]]>
    </select>

    <!-- Reservation Insert -->
    <insert id="insertReservation" parameterType="com.teamcommit.kickoff.Do.ReservationDO">
        <![CDATA[
            INSERT INTO RESERVATION (
                RESERVATION_NO,
            /*    USER_ID,
                PLACE_ID,
                PLACE_NAME,*/
                RESERVATION_COURT_NAME,
             /*   PLACE_ADDRESS,*/
                RESERVATION_COURT_FORM,
                RESERVATION_DATE,
                RESERVATION_START_TIME,
                RESERVATION_END_TIME,
                RESERVATION_STATUS,
                RESERVATION_PRICE,
                RESERVATION_COMMENT,
                RESERVATION_REG_DATE
            )
            VALUES (
                RESERVATION_SEQ.NEXTVAL,
                #{reservationCourtName},
                #{reservationCourtForm},
                #{reservationDate},
                #{reservationStartTime},
                #{reservationEndTime},
                'Y',
                #{reservationPrice},
                #{reservationComment},
                SYSDATE
            )
        ]]>
    </insert>

    <!-- Reservation Detail -->
    <select id="selectReservationDetail" parameterType="com.teamcommit.kickoff.Do.ReservationDO" resultType="com.teamcommit.kickoff.Do.ReservationDO">
        <![CDATA[
            SELECT *
            FROM
                RESERVATION
            WHERE
                RESERVATION_NO = #{reservationNo}
        ]]>
    </select>

    <!-- Reservation procSetUserInfo -->
    <select id="procSetUserInfo" parameterType="com.teamcommit.kickoff.Do.EmployerDO" resultType="com.teamcommit.kickoff.Do.EmployerDO">
        <![CDATA[
            SELECT *
            FROM
                EMPLOYER
            WHERE
                EMP_ID = #{empId}
        ]]>
    </select>

</mapper>
